{
  "name": "node-yasdi",
  "version": "1.0.0",
  "description": "Native module for YASDI on NodeJS",
  "dependencies": {
    "bindings": "^1.3.0",
    "cmake-js": "2",
    "nan": "2"
  },
  "scripts": {
    "test": "mocha ./tests",
    "install": "cmake-js compile -d projects/generic-cmake/ -O build"
  },
  "devDependencies": {
    "mocha": "*"
  }
}
